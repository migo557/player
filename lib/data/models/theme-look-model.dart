import 'package:flex_color_scheme/flex_color_scheme.dart';

class ThemeLookModel {
  String title;
  FlexScheme flexScheme;
  final FlexSchemeData flexColor;
  ThemeLookModel({
    required this.title,
    required this.flexScheme,
    required this.flexColor
  });
}
