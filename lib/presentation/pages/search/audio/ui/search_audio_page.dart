import 'package:flutter/material.dart';

class SearchAudioPage extends StatelessWidget {
  const SearchAudioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Audio Search Page"),
      ),
    );
  }
}
