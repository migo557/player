import 'package:flutter/material.dart';

class SearchVideosPage extends StatelessWidget {
  const SearchVideosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Videos Search Page"),
      ),
    );
  }
}
